apiVersion: v1 
kind : Service
metadata:
  name: flight-price-service
spec:
  type: NodePort
  selector:
    app: flightpricepredictor

  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080